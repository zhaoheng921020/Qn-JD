# 出库托盘接口修复说明

## 问题描述
实际请求URL变成了：
```
http://8.137.162.161:8080/api/companies//outbound-tray-authorizations
```
出现了双斜杠 `//`，原因是 `storehouseId` 为空。

## 修复内容

### 1. 设置默认仓库
在 `searchWarehouse()` 方法中，获取仓库列表后自动设置第一个仓库为默认值：

```javascript
searchWarehouse() {
    this.request('api/companies/enabled', {...})
    .then(res => {
        if (res.data && res.data.length > 0) {
            this.allWarehouse = res.data;
            // 设置默认仓库（第一个）
            const defaultWarehouseId = res.data[0].id;
            this.listPostData.storehouseId = defaultWarehouseId;
            this.changeWarehouse();
        }
    });
}
```

### 2. 添加空值检查
在以下方法中添加了 `storehouseId` 空值检查：

- `changeWarehouse()` - 确保 storehouseId 存在才执行查询
- `loadTrayData()` - 确保 storehouseId 存在才加载托盘数据
- `loadOutboundTrays()` - 确保 storehouseId 存在才加载出库托盘

```javascript
// 确保storehouseId存在才执行查询
if (!this.listPostData.storehouseId) {
    console.warn('仓库ID为空，跳过查询');
    return;
}
```

### 3. 修正URL格式
所有涉及 `storehouseId` 的URL都使用正确的格式：

```javascript
const url = `api/companies/${storehouseId}/outbound-tray-authorizations`;
```

## 修复后的请求URL示例

正确的请求URL：
```
http://8.137.162.161:8080/api/companies/123/outbound-tray-authorizations
```

其中 `123` 是实际的仓库ID。

## 验证步骤

1. 打开浏览器控制台（F12）
2. 刷新页面，查看网络请求
3. 确认请求URL格式正确，没有双斜杠 `//`
4. 查看控制台日志，确认默认仓库ID已设置

## 其他优化

- 删除了冗余变量：`outList`、`outListPostlist`、`allowOutboundTrays`、`disallowOutboundTrays`
- 重构方法命名：`OutTraySearch()` → `loadOutboundTrays()`，更语义化
- 添加统一的错误处理和提示
- 使用 `Object.assign()` 简化对象属性重置